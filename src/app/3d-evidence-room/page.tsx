import { EvidenceRoom3DClient } from "./EvidenceRoom3DClient";

export const metadata = {
  title: "3D Evidence Room",
};

export default function EvidenceRoom3DPage() {
  return <EvidenceRoom3DClient />;
}
