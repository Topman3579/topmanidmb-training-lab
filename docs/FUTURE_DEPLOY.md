# Future Deploy (not configured)

Reserved domain: **lab.topmanidmb.studio**

This prototype does **not** include:

- DNS records
- nginx / Cloudflare config
- CI/CD pipelines
- Production environment variables

When ready, deploy the static `out/` folder to an isolated host separate from production TOPMANIDMB services.